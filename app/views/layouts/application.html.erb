<!DOCTYPE html>
<html>
<head>
  <title>New Zealand Land and Waste</title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag 'fips', 'd3/d3.min.js', 'd3/d3.geo.min.js', 'd3/d3.geom.min.js', 'protovis/protodata.js', 'polymaps.min.js' %>

  <%= csrf_meta_tag %>
</head>
<body>
<div class="topbar">
      <div class="topbar-inner">
        <div class="container">
          <h3><%= link_to 'NZ-LW'.html_safe, root_path %></h3>
          <ul class="nav">
            <li class="<%= 'active' if current_page?(regions_url) %>">
              <%= link_to 'Regions', regions_url %>
            </li>
            <li class="<%= 'active' if current_page?(land_url) %>">
              <%= link_to 'Land', land_url %>
            </li>
            <li class="<%= 'active' if current_page?(waste_url) %>">
              <%= link_to 'Waste', waste_url %>
            </li>
          </ul>
          <ul class="nav secondary-nav">
            <li><%= link_to 'About', about_path(:anchor => 'about') %></li>
            <li><%= link_to 'Sources', about_path(:anchor => 'sources') %></li>
          </ul>
        </div>
      </div><!-- /topbar-inner -->
    </div>

  <div id="root_container" class="container">
    <%= yield %>
  </div>

</body>
</html>
